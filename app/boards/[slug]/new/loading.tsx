import { LoadingState } from "@/components/design-system";

export default function BoardNewPostLoadingPage() {
  return <LoadingState title="글쓰기 화면을 준비하는 중입니다" body="게시판에 맞는 입력 항목을 확인하고 있습니다." />;
}
